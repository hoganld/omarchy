# Menus
bindd = $HYPER, SPACE, Launch apps, exec, omarchy-launch-walker
bindd = $HYPER CTRL, E, Emoji picker, exec, omarchy-launch-walker -m symbols
bindd = $HYPER ALT, SPACE, Omarchy menu, exec, omarchy-menu
bindd = $HYPER, ESCAPE, System menu, exec, omarchy-menu system
bindld = , XF86PowerOff, Power menu, exec, omarchy-menu system
bindd = $HYPER, SEMICOLON, Show key bindings, exec, omarchy-menu-keybindings # alternatively, Hyper+Shift+K ?

# Aesthetics
bindd = $HYPER SHIFT, SPACE, Toggle top bar, exec, omarchy-toggle-waybar
bindd = $HYPER CTRL, SPACE, Next background in theme, exec, omarchy-theme-bg-next
bindd = $HYPER SHIFT CTRL, SPACE, Theme menu, exec, omarchy-menu theme
bindd = $HYPER, BACKSPACE, Toggle window transparency, exec, hyprctl dispatch setprop "address:$(hyprctl activewindow -j | jq -r '.address')" opaque toggle
bindd = $HYPER SHIFT, BACKSPACE, Toggle workspace gaps, exec, omarchy-hyprland-workspace-toggle-gaps

# Notifications
bindd = $HYPER, COMMA, Dismiss last notification, exec, makoctl dismiss
bindd = $HYPER SHIFT, COMMA, Dismiss all notifications, exec, makoctl dismiss --all
bindd = $HYPER CTRL, COMMA, Toggle silencing notifications, exec, makoctl mode -t do-not-disturb && makoctl mode | grep -q 'do-not-disturb' && notify-send "Silenced notifications" || notify-send "Enabled notifications"
bindd = $HYPER ALT, COMMA, Invoke last notification, exec, makoctl invoke
bindd = $HYPER SHIFT ALT, COMMA, Invoke last notification, exec, makoctl restore

# Toggle idling
bindd = $HYPER CTRL, I, Toggle locking on idle, exec, omarchy-toggle-idle

# Toggle nightlight
bindd = $HYPER CTRL, N, Toggle nightlight, exec, omarchy-toggle-nightlight

# Captures
bindd = , PRINT, Screenshot with editing, exec, omarchy-cmd-screenshot
bindd = SHIFT, PRINT, Screenshot to clipboard, exec, omarchy-cmd-screenshot smart clipboard
bindd = ALT, PRINT, Screenrecording, exec, omarchy-menu screenrecord
bindd = $HYPER, PRINT, Color picker, exec, pkill hyprpicker || hyprpicker -a

# File sharing
bindd = $HYPER CTRL, S, Share, exec, omarchy-menu share

# Waybar-less information
bindd = $HYPER CTRL, T, Show time, exec, notify-send "    $(date +"%A %H:%M  —  %d %B W%V %Y")"
bindd = $HYPER CTRL, B, Show battery remaining, exec, notify-send "󰁹    Battery is at $(omarchy-battery-remaining)%"

# Control panels
bindd = $HYPER CTRL, A, Audio controls, exec, omarchy-launch-audio
bindd = $HYPER CTRL, B, Bluetooth controls, exec, omarchy-launch-bluetooth
bindd = $HYPER CTRL, W, Wifi controls, exec, omarchy-launch-wifi
bindd = $HYPER CTRL, T, Activity, exec, omarchy-launch-tui btop

# Dictation
bindd  = $HYPER CTRL, X, Start dictation, exec, voxtype record start
binddr = $HYPER CTRL, X, Stop dictation, exec, voxtype record stop

# Lock system
bindd = $HYPER CTRL, L, Lock system, exec, omarchy-lock-screen

# Toggle Language Input
bindd = , Hiragana_Katakana, IME, exec, omarchy-toggle-language
