! layout = keycodes
  usa = evdev+aliases(qwerty)
  code = evdev+aliases(qwerty)
  mac = evdev+aliases(qwerty)
  launch = evdev+aliases(qwerty)

! layout = types
  usa = complete
  code = complete
  mac = complete
  launch = complete

! layout = compat
  usa = complete
  code = complete
  mac = complete
  launch = complete

! layout = symbols
  usa = kog(usa)
  code = kog(code)
  mac = kog(mac)
  launch = kog(launch)

! include %S/evdev
